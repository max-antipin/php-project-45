#!/usr/bin/env php
<?php

declare(strict_types=1);

foreach (['vendor', '../..'] as $i => $dir) {
    $file = __DIR__ . "/../$dir/autoload.php";
    if ($i || file_exists($file)) {
        require_once $file;
        break;
    }
}

use function BrainGames\Cli\Games\EvenGame\run;

run();
